/**
 * Placeholder for future R3F/WebGL post-processing (chromatic aberration/scanlines).
 * Keep this OFF by default and code-split when used.
 * DO NOT import three/r3f until feature flag is explicitly enabled to avoid bundle bloat.
 */
export default function WebglFxStub() {
  return null;
}